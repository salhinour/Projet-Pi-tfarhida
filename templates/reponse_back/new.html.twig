{% extends 'baseback.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">
                        <h1 class="card-title">Create new Reponse</h1>
                    </div>
                    <div class="card-body">
                        {{ include('reponse_back/_form.html.twig') }}
                    </div>
                </div>
                <div class="mt-3">
                    <a href="{{ path('app_reponse_back_index') }}" class="btn btn-secondary">Back to List</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
