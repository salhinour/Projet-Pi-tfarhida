<form method="post" action="{{ path('app_activitee_delete', {'id': activitee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activitee.id) }}">
    <button class="btn">Delete</button>
</form>
