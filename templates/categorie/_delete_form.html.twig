<!DOCTYPE html>
<html lang="fr">
<head>
    <!-- Mettez ici les autres balises meta, titre et liens vers les autres fichiers CSS -->
    <style>
        /* CSS pour le bouton Save */
        .btn-save {
            background-color: orange; /* Couleur orange */
            color: white;
            border: 2px solid orange; /* Bordure orange */
            border-radius: 5px;
            padding: 8px 12px;
            cursor: pointer;
        }

        .btn-save:hover {
            background-color: darkorange; /* Couleur orange foncé au survol */
            border-color: darkorange; /* Bordure orange foncée au survol */
        }

        /* CSS pour le bouton Delete */
        .btn-delete {
            background-color: blue; /* Nouvelle couleur bleue */
            color: white;
            border: 2px solid blue; /* Bordure bleue */
            border-radius: 5px;
            padding: 8px 12px;
            cursor: pointer;
        }

        .btn-delete:hover {
            background-color: darkblue; /* Couleur bleue foncée au survol */
            border-color: darkblue; /* Bordure bleue foncée au survol */
        }
    </style>
</head>
<form method="post" action="{{ path('app_categorie_delete', {'id': categorie.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie.id) }}">
    <button class="btn-delete">Delete</button>
</form>

    <!-- Votre autre contenu HTML ici -->
</body>
</html>



